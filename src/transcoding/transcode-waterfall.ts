/**
 * Transcoding implementation - Waterfall pattern
 * (How Katana does it)
 */

export async function transcodeWaterfall(file: File): Promise<Blob> {
  // TODO: Implement waterfall pattern
  // - Explanation of this approach

  return new Blob([], { type: "video/mp4" });
}
